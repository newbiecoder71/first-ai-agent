# yaml-language-server: $schema=none
services:
  - type: web
    name: first-ai-agent
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: ./start.sh
    envVars:
      - key: OPENAI_API_KEY
        sync: false
